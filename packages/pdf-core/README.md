# packages/pdf-core

Placeholder for extracting `apps/api/src/pdf/*` into a standalone,
publishable package if you want to reuse the PDF processing functions
outside this API (e.g. in a CLI tool or a serverless function).
