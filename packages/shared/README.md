# packages/shared

Placeholder for code shared between apps/web, apps/mobile, and apps/api
(e.g. TypeScript types for the Job/Tool contracts). Not yet extracted —
today those types are duplicated in `apps/api/src/services/jobStore.ts`
and `apps/web/js/tools-config.js` / `apps/mobile/src/api/toolsConfig.ts`.
