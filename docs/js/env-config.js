// Set this to your deployed API's base URL when hosting the web app
// separately from the API (e.g. web on Vercel, API on Render/Fly/Railway).
// Locally with docker-compose or `npm run dev`, the default is correct.
window.PDF_MASTER_API_BASE = window.PDF_MASTER_API_BASE || 'https://pdf-master-pro-api-fss5.onrender.com/api';
